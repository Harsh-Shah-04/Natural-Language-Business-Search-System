[project]
name = "business-search-backend"
version = "0.1.0"
description = "Natural language business search — FastAPI backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pymongo>=4.9",
    "python-dotenv>=1.0",
]
